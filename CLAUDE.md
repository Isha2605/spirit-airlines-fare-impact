# Spirit Airlines Fare Impact Case Study - Claude Guidelines

## Project Context (Prevent Hallucination)

**Project:** Analyze fare impact of Spirit Airlines shutdown (May 2, 2026) on 20 busiest US routes

**Core Question:** How much did airfares rise after Spirit shut down?

**Status:** Data collected + analysis notebook created. Building Flask dashboard + PowerPoint deck.

---

## VERIFIED DATA SOURCES (Ground Truth - DO NOT HALLUCINATE)

### Data Files in Workspace
- `data/processed/spirit_20routes_clean.csv` - BTS Q1 2025 (Spirit/NK only)
- `data/processed/spirit_20routes_clean_q2.csv` - BTS Q2 2025 (Spirit/NK only)
- `data/processed/google_flights_fares.csv` - Current fares (May 8, 2026 search)

**Columns:**
- BTS: YEAR, QUARTER, ORIGIN, DEST, OPERATING_CARRIER, PASSENGERS, MARKET_FARE
- GF: Route, Origin, Destination, Search_Date, Travel_Date, Current_fare, Carrier

### The 20 Routes (EXACT - Do NOT Modify)
```
FLL→LGA, FLL→ORD, FLL→ATL, FLL→DFW, FLL→LAX,
MCO→LGA, MCO→ORD, MCO→BWI,
LAS→DFW, LAS→LAX, LAS→ATL, LAS→IAH,
IAH→FLL, IAH→MCO,
DTW→FLL, DTW→MCO,
BWI→FLL, PHL→MCO, ATL→FLL, ORD→FLL
```

### Key Observation (Verified)
**Frontier Airlines now serves most of Spirit's routes** - This is your counterfactual story (don't make up other carriers).

---

## Four Core Principles

### 1. THINK BEFORE CODING
**State assumptions explicitly. Ask clarifying questions. Surface tradeoffs.**

- If task is ambiguous, ask which approach you prefer (Tableau vs Flask, static vs dynamic, etc.)
- If data seems inconsistent, flag it before proceeding
- If I suggest multiple options, ask which one you want
- Don't assume the goal—confirm first

**Example:**
- ❌ Bad: "I'll build a Flask app with 10 interactive filters"
- ✅ Good: "Should the dashboard prioritize simplicity (5 KPIs + 3 charts) or deep interactivity (10+ filters)?"

### 2. SIMPLICITY FIRST
**Minimum code that solves the problem. No speculation.**

- Build only what was explicitly requested
- Don't add "nice-to-have" features without asking
- Dashboard = 6 KPI cards + 5 charts (not 20 charts you *might* want)
- No unnecessary abstractions or helper classes
- If 200 lines of Flask code can be 100, rewrite it

**For this project:**
- Dashboard: KPI cards + fare impact heatmap + carrier bar chart + passenger scatter + timeline
- Don't add: export to PDF, email alerts, user authentication, admin panels

### 3. SURGICAL CHANGES
**Touch only what you must. Match existing style.**

- When editing dashboard.html, don't refactor the entire template
- When adding Python code, match the existing style in notebooks/
- Don't "improve" code that wasn't asked to be improved
- If I notice dead code, mention it—don't delete it unless you ask

**For this project:**
- If you ask for a dashboard: build the Flask app, don't rewrite the analysis notebook
- If you ask for the deck: don't also refactor the analysis code

### 4. GOAL-DRIVEN EXECUTION
**Define success criteria. Verify completion before moving on.**

**For multi-step tasks, I will state:**
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

**For this project:**
- ✅ Analysis notebook → Verify: CSVs generated (fare_impact_analysis.csv, carrier_analysis.csv, kpi_summary.csv)
- ✅ Flask dashboard → Verify: Runs locally on port 5000, shows 6 KPI cards + 4 charts, no errors
- ✅ PowerPoint deck → Verify: 8 slides, narrative complete, ready for LinkedIn

---

## PROJECT-SPECIFIC GUIDELINES

### 1. Data References
**ALWAYS CHECK:** Is the data in the workspace before using it?
- `data/processed/` exists and has the three CSV files
- Don't assume column names—check actual CSVs first
- If column is missing, flag it and don't hallucinate data

### 2. Analysis Notebook (COMPLETE - READ BEFORE ADDING)
**File:** `notebooks/03_analysis.ipynb`

**What it does:**
1. Loads BTS Q1 + Q2 Spirit data
2. Calculates avg fare per route (cents → dollars)
3. Loads Google Flights May 2026 data
4. Merges on route, calculates % change
5. Exports 3 CSVs: fare_impact_analysis.csv, carrier_analysis.csv, kpi_summary.csv

**DON'T modify this notebook** unless you ask. If you need different outputs, extend it—don't replace.

### 3. Dashboard Build (Flask + Plotly + Bootstrap)
**Tech Stack (FIXED - Don't suggest alternatives without asking):**
- Backend: Flask (Python)
- Frontend: Plotly (interactive charts) + Bootstrap/Gentelella template
- Data: Read from CSVs generated by analysis notebook

**Structure (No deviation without asking):**
```
app.py                          # Flask routes
templates/
  - dashboard.html              # Main dashboard page
static/
  - css/style.css              # Custom styles
  - js/charts.js               # Plotly chart definitions
data/
  - fare_impact_analysis.csv    # Input data
  - carrier_analysis.csv
  - kpi_summary.csv
```

**KPIs to Display (Exact list):**
1. Average fare increase (%)
2. Median fare increase (%)
3. Average fare increase ($)
4. # routes with 30%+ increase
5. # routes with 20-30% increase
6. Highest impact route

**Charts to Include (Exact list):**
1. Bar chart: Top 10 routes by % increase
2. Heatmap or bubble scatter: Routes colored by carrier
3. Line chart: Fare trajectory over time
4. Pie/bar chart: Carrier market share on Spirit routes

### 4. PowerPoint Deck (8 Slides - FIXED)
**Don't suggest alternatives (Canva, Google Slides, Figma)** unless the current approach doesn't work.

**Slide Outline:**
1. Title: "Spirit Airlines Shutdown: The Fare Impact"
2. Context: Why Spirit matters, metrics
3. Methodology: Data sources, caveats
4. Headline: Avg fare increase (%)
5. Route impact: Top 5 hardest hit
6. Market concentration: Carrier shifts, Frontier dominance
7. Insights: Oligopoly risk, consumer impact
8. CTA: Link to dashboard + GitHub

### 5. Token Optimization Rules
**To prevent unnecessary token waste:**

- Don't repeat the entire CLAUDE.md in responses
- Don't explain the same concept twice in one conversation
- When referring to the analysis notebook, don't paste all 13 cells—summarize
- Use inline code references (e.g., `fare_impact_analysis.csv`) instead of full paths
- If task is clear, move to execution immediately (don't ask "should I?")

### 6. Success Criteria (Clear & Verifiable)

| Task | Success = |
|------|-----------|
| **Analysis** | Run notebook → 3 CSVs exist in `data/processed/` with correct columns |
| **Flask Dashboard** | `python app.py` → localhost:5000 loads → 6 KPI cards visible + 4 charts rendered |
| **PowerPoint** | 8 slides with data-backed visuals + narrative arc complete |
| **LinkedIn Posts** | 2 posts drafted with copy + links + hashtags |

---

## Questions I Will Ask (Before Coding)

✅ **I WILL ASK:**
- "Should the dashboard be interactive (filters) or static snapshot?"
- "For the deck, do you want slide notes/speaker notes?"
- "Should the Flask app auto-refresh data or use static CSVs?"
- "Do you want the dashboard deployed to a server or run locally?"

❌ **I WON'T ASK:**
- "Is pandas installed?" (I'll just install it)
- "Should I create the folder structure?" (I'll just do it)
- "Do you want Python 3.9 or 3.10?" (I'll use what's available)

---

## Current Status Tracker

```
[✅] Data collected (BTS Q1-Q2 + Google Flights)
[✅] Analysis notebook created (ready to run)
[⏳] Analysis notebook executed → CSVs generated
[⏳] Flask dashboard built
[⏳] PowerPoint deck created
[⏳] LinkedIn posts drafted
[⏳] Dashboard deployed (optional)
```

---

## Red Flags (Stop & Clarify)

🚩 **If I suggest something outside this scope** → Push back. Examples:
- "We should add ML predictions" → Why? Not in scope.
- "Let's build a real-time data pipeline" → Unnecessary complexity.
- "Maybe add 10 more dashboard tabs" → Violates Simplicity First.

🚩 **If data doesn't match expectations** → Flag immediately:
- Missing columns in CSV → List what's missing
- Fare values look wrong (e.g., $5 vs $500) → Ask before proceeding
- Routes don't match the 20-route list → Highlight discrepancy

🚩 **If you don't provide clear success criteria** → Ask for them
- "Make the dashboard better" → Too vague, need specifics
- "Build the presentation" → How many slides? What format? What data?

---

## How to Work With This File

**When asking me to code:**
1. Provide a clear, specific task
2. Reference this file: "Build the Flask dashboard per CLAUDE.md"
3. I will execute per the 4 principles
4. I will ask clarifying questions if needed
5. I will verify success using the criteria above

**Example good prompt:**
> "Build the Flask dashboard with the 6 KPIs and 4 charts listed in CLAUDE.md. Use the CSVs from the analysis notebook output."

**Example bad prompt:**
> "Make a nice dashboard"
> → I will ask: Nice = interactive? Static? How many charts?

---

## License & Attribution

This file combines [Karpathy-inspired Claude guidelines](https://github.com/multica-ai/andrej-karpathy-skills) with project-specific rules for Spirit Airlines case study.
